### No. 4-5
- **テストケース名**: メール形式が不正で会員登録


- **前提条件**: なし


- **手順**:`HTTP POST`リクエストを `/api/customers/register` に送信。


- **入力データ**:json
{ "name": "不正太郎", "email": "invalid-email", "password": "123" }
- **期待結果**:


  - `HTTP`ステータスコードが `400 Bad Request` であること。


  - エラーメッセージが返され、登録処理は実行されない。