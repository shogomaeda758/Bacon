## テストシナリオ

### No. 16

- **テストケース名**: カテゴリー別商品取得（存在しないカテゴリー）
- **前提条件**:
  - データベースの `categories` テーブルに `categoryId = 999` のデータが存在しない。
- **手順**:
  1. HTTP GETリクエストを `/api/products/category/999` エンドポイントに送信する。
- **入力データ**:
  - パスパラメータ: `categoryId = 999`
- **期待結果**:
  1. HTTPステータスコードが `200 OK` であること。
  2. レスポンスボディがJSON形式の空の配列 `[]` であること。