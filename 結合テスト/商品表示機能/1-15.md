## テストシナリオ

### No. 15

- **テストケース名**: 商品検索（空のキーワード）
- **前提条件**：
  - 特になし（DBの状態によらない）。
- **手順**:
  1. HTTP GETリクエストを `/api/products/search?keyword=` エンドポイントに送信する。
- **入力データ**：
  - クエリパラメータ: `keyword = ""`（空）
- **期待結果**：
  1. HTTPステータスコードが `400 Bad Request` であること。
  2. レスポンスボディにエラー情報（例：空のキーワードを示すメッセージ）が含まれること。
  3. `ProductService.searchProducts` メソッドが呼び出されないこと。