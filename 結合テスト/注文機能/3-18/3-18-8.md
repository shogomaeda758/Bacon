# 機能：注文機能

## テスト対象API

- `POST /api/orders`

## テストデータ準備方針：

- テストの独立性を保つため、各テストケース実行前にデータベースをクリーンな状態にし、テストケースごとに必要な商品データを投入することを想定します。
- 以下では、テストデータとして投入される商品情報を `Product(ID, Name, Price, Desc, Stock, ImageUrl, IsRecommended)` の形式で記述します。

## テストシナリオ

### No. 3-18-8

- テストケース名: 注文（異常系 - 顧客情報の形式が不正、電話番号が9桁）
- 前提条件:
    - DBにテスト商品として以下の商品が登録されていること:
        - `Product(1, "シンプルデスクオーガナイザー", 3500, "机の上をすっきり整理できる木製オーガナイザー。", 20, "/images/desk-organizer.png", true)`
    - セッションに以下のカート情報が存在すること:
        - `Cart(items={"1": CartItem(productId=1, quantity=1, ...)})`

- 手順:
    1. HTTP POSTリクエストを `/api/orders` エンドポイントに送信する。
        - **HTTPリクエスト**: `POST /api/orders`
        - **リクエストボディ**:電話番号が9桁、入力データ参照
            ```
        - **セッション**: 上記前提条件のカート情報


- 入力データ:
    ```json
{
"customerInfo": {
"name": "顧客　太郎",
"email": "test-member-a@example.com",
"address": "東京都品川区",
"phoneNumber": "012345678"
},
"paymentMethod": "credit_card", 
"items": [
{
"productId": 1,
"name": "シンプルデスクオーガナイザー",
"price": 3500,
"quantity": 1,
"subtotal": 3500
}
],
"totalPrice": 3500,
"shippingFee": 500,
"grandTotal": 4000
}
    ```

- 期待結果:
    1. HTTPステータスコードが **`400 Bad Request`** であること。
    2. レスポンスボディがJSON形式であり、`customerInfo.email` に関するバリデーションエラーメッセージ（ `"電話番号は10桁または11桁の数字で入力してください。"` など、`OrderRequest` DTOのバリデーションメッセージに依存）を含む `OrderResponse` であること。複数のエラーがある場合は `;` で連結されること。
    3. `OrderController` の `placeOrder` メソッドに到達するが、`@Valid` アノテーションによるバリデーションで `MethodArgumentNotValidException` が発生し、`handleValidationExceptions` メソッドで処理されること。`OrderService` は呼び出されないこと。
    4. DB状態変化: なし。
    5. セッション状態変化: なし。