### No.2-9
- テストケース名: カート追加時に数量0を指定する
- 前提条件:
  - 特になし。
- 手順:
  1. HTTP POSTリクエストを `/api/cart` に送信する。
- 入力データ:
  - リクエストボディ(JSON): `{"productId": 1, "quantity": 0}`
- 期待結果:
  1. HTTPステータスコードが `400 Bad Request` であること。
  2. レスポンスに `"数量は1以上で入力してください。"` のバリデーションエラーが含まれること。
  3. カート情報は更新されないこと。