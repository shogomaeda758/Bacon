### No.2-12
- テストケース名: カート追加時に商品IDをnullで指定する
- 前提条件:
  - 特になし。
- 手順:
  1. HTTP POSTリクエストを `/api/cart` に送信する。
- 入力データ:
  - リクエストボディ(JSON): `{"productId": null, "quantity": 1}`
- 期待結果:
  1. HTTPステータスコードが `400 Bad Request` であること。
  2. レスポンスに `"商品IDは必須です。"` のエラーメッセージが含まれること。
  3. カート情報は更新されないこと。